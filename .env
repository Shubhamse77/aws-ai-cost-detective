# Optional: Set default region for global calls like Cost Explorer / S3
AWS_DEFAULT_REGION=ap-south-2

# Optional LLM choice (for upcoming phases)
LLM_PROVIDER=openai  # choices: bedrock, openai, gemini
OPENAI_API_KEY=your_openai_api_key_here
